var assert = require('assert');

describe('Test to test Mocha', function() {
  describe('This is a test', function() {
    it('should always return true', function() {
      assert.equal(1, 1);
    });
  });
});