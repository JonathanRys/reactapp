# AWS deployment plan 

dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync home\ubuntu\jonathanrys http://jonathanrys.s3-website.us-east-1.amazonaws.com/ --delete