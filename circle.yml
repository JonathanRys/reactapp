# AWS deployment plan 
# test change 1
    deployment:
      staging:
        branch: master
        commands:
          - aws s3 sync ./build/ http://jonathanrys.s3-website.us-east-2.amazonaws.com/ --delete
      production:
        branch: master
        commands:
          - aws s3 sync ./build/ http://jonathanrys.s3-website.us-east-2.amazonaws.com/ --delete
